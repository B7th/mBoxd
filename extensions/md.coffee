md = require 'marked'

module.exports = (name, imports, write) =>
  # Export as html with pug based on default
  
module.exports.ext = 'html' #? Same for emails? And if they are under L or have a .[lang].md?