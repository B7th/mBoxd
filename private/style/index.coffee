Coffee

Anchors and anchor lists
m:ancs full*|collapse
change of id part of anchor list at top of screen? Change focus on anchor

m('ancs')

$(m('ancs')).each (e) ->
  

$('[type="file"]').onchange , (e) -> 
  

$(m('tbl')).parent().css 'overflow', 'scroll'

// Images as backgrounds fading in and out as a mask from a folder